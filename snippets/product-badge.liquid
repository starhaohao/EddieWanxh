{% comment %}
  Renders a badge

  Accepts:
  - badge_class: {String} the class of the badge (required)
  - badge_text: {String} the text of the badge (required)
  - aos_delay: {String} the delay for the AOS animation (required)
  - aos_duration: {String} the duration for the AOS animation (required)
  - aos_anchor: {String} the anchor for the AOS animation (required)
  - badge_style: {String} optional inline CSS variables for badge colors

  Usage:
  {% render 'product-badge', badge_class: badge_class, badge_text: badge_text, aos_delay: aos_delay, aos_duration: aos_duration, aos_anchor: aos_anchor, badge_style: badge_style %}
{% endcomment %}

<span
  class="{{ badge_class }}"
  {% if badge_style != blank %}
    style="{{ badge_style }}"
  {% endif %}
  data-aos="fade"
  data-aos-delay="{{ aos_delay }}"
  data-aos-duration="{{ aos_duration }}"
  data-aos-anchor="{{ aos_anchor }}"
>
  {{ badge_text }}
</span>
